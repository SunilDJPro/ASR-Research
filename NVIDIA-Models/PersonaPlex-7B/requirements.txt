# NVIDIA PersonaPlex Demo - Requirements
# Tested on Ubuntu 24.04 with CUDA 12.x and A6000 48GB

# Core ML
torch>=2.1.0
torchaudio>=2.1.0

# HuggingFace
huggingface_hub>=0.20.0
safetensors>=0.4.0

# PersonaPlex/Moshi dependencies
# Install from NVIDIA repo: pip install git+https://github.com/NVIDIA/personaplex.git#subdirectory=moshi
# Or clone and install locally

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.11.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0

# System dependency (install via apt):
# sudo apt install libopus-dev